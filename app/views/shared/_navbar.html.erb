<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "g<span class='ruby'>ruby</span>blog.pl".html_safe, root_path, :class => "navbar-brand logo" %>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <%= form_tag(root_path, :method => "get", :class => "navbar-form navbar-right", :role => "search" ) do %>
      <%= text_field_tag :search, params[:search], :id => "search", :class => "form-control", :placeholder => "Szukaj" %>
      <button type="submit" class="btn btn-default"><%= image_tag("find.png") %></button>
    <% end %>
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= yield(:home) %>"><%= link_to "Home", root_path %></li>
        <li class="dropdown <%= yield(:categories) %>">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Kategorie <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
          <% Category.all.each do |category| %>
            <li><%= link_to category.name, category_path(category) %></li>
          <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>